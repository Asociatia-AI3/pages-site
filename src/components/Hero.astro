---
import Button from "./Button.astro";
import { Image } from "astro:assets";
import logo_gem from "../../public/logo_gem.svg";
import heroBg from "../../public/heroBg.jpeg";
---

<div class="relative">
  <div class="mx-auto max-w-7xl py:24 sm:py-36">
    <div class="lg:grid lg:grid-cols-2 lg:gap-24">
      <div
        class="mx-auto px-6 sm:max-w-2xl sm:text-center lg:flex lg:items-center lg:px-0 lg:text-left z-50"
      >
        <div class="lg:py-24">
          <h1
            class="text-5xl font-semibold text-accentt sm:text-6xl lg:mt-6 xl:text-6xl leading-10"
          >
            Inițiativă pentru dezvoltare durabilă
          </h1>
          <p class="mt-3 text-lg text-secondary sm:mt-5 max-w-md">
            Fă cunoștință cu <strong
              >primul cluster de IT&C din județul Alba!</strong
            > Încurajăm, dezvoltăm și generăm oportunități pentru industriile high-tech,
            împreună.
          </p>

          <div class="mt-10 sm:mt-12">
            <Button
              href="#member"
              text="Devino membru"
            />
          </div>
        </div>
      </div>
      <div class="relative">
        <Image
          src={heroBg}
          alt=""
          class="rounded-tl-[110px] rounded-lg"
        />
        <Image
          src={logo_gem}
          alt=""
          class="absolute -bottom-1 -left-12"
        />
      </div>
    </div>
  </div>
</div>
